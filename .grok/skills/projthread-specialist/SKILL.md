---
name: projthread-specialist
description: Use when this agent has the ProjThread repository and is dogfooding or distributing the product into a workspace (empty pins, first guides, bootstrap). Use when running /projthread-specialist. Do not use for Grok Bot or worker-only MCP. Do not use as the map for implementing a STATUS slice.
---

# ProjThread specialist

You have this **repo**. Worker agents do not. Hand them `using-projthread` only.

**REQUIRED:** `using-projthread` for product physics. Do not restate it here.

**Implementing the Worker** is a different mode: `AGENTS.md` → `docs/STATUS.md`. This skill is not that map. Do not invent a STATUS slice from dumps or from an empty wiki.

## Distribute

When pins are empty and the human wants a place that feels like ProjThread (not a blank board):

- Seed **one or two** product-shaped guide nodes. Teach designed use as named in `using-projthread` (stream, densities, capture as ingest). Short. Guiding, not an org chart, not a bot tree, not a private pair as law.
- `attach_node_project` onto the root project if it helps Workers find the guides. Do not file setup **cards**.
- MCP cannot pin. Ask the human to pin those nodes in the PWA wiki.
- Tenant process (this org’s habits) grows later on wiki as they work. Do not plant Farm or a fake company.

Load this skill **with** `using-projthread` when collaborating in a workspace. Load `AGENTS.md` when coding a named slice.
