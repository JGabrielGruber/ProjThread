# Status

Update this file when a slice lands. Do not put slice history in root `AGENTS.md`.

**Live:** local wrangler — empty-tenant setup (session, zero memberships → create workspace); Farm seed retired from the repo; kanban moves (`stage_changed`), room chat + Activity markers on DO tape (reconnect last_seq), Activity-only from D1, wiki markdown **or json** nodes + work-item links (no promote) + **blob HTTP** (multipart `POST /api/workspaces/:ws/nodes` `payload_kind=blob` + `file`; `GET`/`PUT /api/nodes/:id/blob`; JSON POST blob still 400; MCP `wiki_read` caption + mime) + PWA wiki blob create + preview-by-mime (`image/*` img, else download) + `node_project` write (`POST /api/nodes/:id/projects`; GET `project_ids`; MCP `attach_node_project`; PWA still filter-only), node–node `includes` + `ref` HTTP (`GET` outline/cites without child content; attach still `node_work_item`), reverse attach `GET`/`POST /api/work-items/:id/nodes`, notify subscriptions (`GET/POST /api/workspaces/:ws/notify-subscriptions`; PATCH/DELETE by id; Queue `NOTIFY` / `projthread-notify`; doorbell `{ kind, node_id, workspace_id }`; Config list/add/remove; MCP `notify_*`; n=1), PWA Config (members add/role/remove, create/rename/reparent projects, stage labels/order, workspace create, notify subscriptions; our Vue, no PrimeVue), session `workspace_id` (picker `PATCH /api/me`; not a URL place), project tree filter chrome on kanban/wiki/room (desktop right; mobile Filters ≤80dvh), room owner picker + nodes of the card, tokenized Grok/X-sharp skin (dark-first + light, `pt-theme`), chrome shell (left rail on desktop, compact top nav on small screens), overlay status toast, wiki create in Modal and edit filling the stage, kanban cards without on-card stage select (Move + reason in Modal), installable PWA (manifest + SW; SW does not intercept `/api/*` or WebSocket), app HTTP `Authorization: Bearer <session.id>` (same D1 session as `pt_session`; no cookie fallback when Bearer is present), admin **Enter as** + **Issue token** (`set_cookie: false`), **capture extension** (Chrome MV3 action popup `dist/extension`; Bearer origin+token; project select/create; report graph markdown + json metadata + `includes` + `node_project`; optional visible-tab PNG blob; no CORS; no cookie), same-origin `/mcp` façade (stateless Streamable HTTP; Bearer only; briefing, wiki/card search, Activity log/recent, bound session workspace or implicit when one membership; node markdown **or json** in `content[0]` (blob: caption in `content[0]`, envelope mime/size/filename, no `blob_key`); Config tools: workspace create, members list/add/role/remove, project create/rename/reparent, stages replace, notify list/add/set/remove; wrap catalog HTTP; `principal_id` not display name), wiki **pins** on `session_briefing` (`{ id, title, type, summary }`, no bodies; human PWA toggle; MCP instructions tell the bot to read pins), PWA routes `/` `/wiki` `/config` `/room/:itemId` (`src/app` pages/components/models/services; `apiJson`; Worker `handleAppShell`; no query-flag rewrite). Remote Worker: https://projthread.jgabrielgruber.workers.dev (D1 + Room DO; Farm **rows** remain; Access on `/admin*` and `/api/admin/*`; chrome shell + toast + wiki edit surface; node-rel `includes`/`ref` HTTP; app HTTP Bearer + admin Issue token; `/mcp` façade with briefing, wiki/card search, Activity, bound session workspace, Config tools; PWA routes + operator CRUD + empty-tenant setup; version `ce62acca-1f40-4198-b67f-b72f95ffcac7`; no PrimeVue; **json nodes not live until José applies `0008` and deploys**; **notify waits on `0009` + queue create + deploy**; **R2 remote bucket `projthread-blobs` not created**; **capture extension is local `npm run build:extension` — not on workers.dev**). Custom-domain Deploy still parked. Remote D1 has `0007_session_workspace` (includes `0006_wiki_pin`); **`0008_payload_kind_json` and `0009_notify` local only**.
**Now:** no open slice. Park Deploy. Do not start OAuth. Do not start room MCP. Do not mint principals. Do not add a PWA people picker. Do not drop remote D1 unless José asks. Do not start **24**. Do not add CORS to the Worker. Do not run `wrangler r2 bucket create` or deploy unless José asks. Do not apply remote `0008`/`0009` or `wrangler queues create` unless José asks. Do not add a second queue.
**Plan:** none
**Next:** write plan **24** PWA share target (not written).
**Parked (product):** Node-rel **HTTP shipped**. Session Bearer **HTTP + admin Issue token shipped**. Catalog MCP **façade shipped** (local + workers.dev). Briefing **pins shipped** (local + workers.dev). Operator CRUD **shipped** (local + workers.dev). Empty-tenant **shipped** (local + workers.dev). Config MCP **shipped** (local + workers.dev). Farm seed file gone. Remote Farm rows remain until José asks. Distinct agent OAuth tokens. Room MCP. Ontology, graph canvas, child rooms stay absences. HTTP-wrap MCP names (replaced). PrimeVue stays out.
**Operator (not a plan):** Reset Grok Bot memory (wrap-era bias). Farm seed file gone; leftover local/remote Farm **rows** wait for José. Do not drop remote D1 unless José asks. Remote `0006_wiki_pin` and `0007_session_workspace` applied. Local `0008_payload_kind_json` and `0009_notify`; do not apply remote unless José asks. Remote queue `projthread-notify` is not created until José asks.
**Landed plans:** `docs/superpowers/plans/2026-08-26-projthread-catalog.md`, `docs/superpowers/plans/2026-08-26-projthread-room.md`, `docs/superpowers/plans/2026-08-26-projthread-activity.md`, `docs/superpowers/plans/2026-08-26-projthread-wiki.md`, `docs/superpowers/plans/2026-08-26-projthread-config.md`, `docs/superpowers/plans/2026-08-27-projthread-skin.md`, `docs/superpowers/plans/2026-08-27-projthread-chrome.md`, `docs/superpowers/plans/2026-08-28-projthread-node-rel.md`, `docs/superpowers/plans/2026-08-28-projthread-session-bearer.md`, `docs/superpowers/plans/2026-08-28-projthread-catalog-mcp.md`, `docs/superpowers/plans/2026-08-28-projthread-catalog-mcp-harden.md`, `docs/superpowers/plans/2026-08-31-projthread-mcp-facade.md`, `docs/superpowers/plans/2026-08-31-projthread-briefing-pins.md`, `docs/superpowers/plans/2026-08-31-projthread-pwa-structure.md`, `docs/superpowers/plans/2026-09-01-projthread-operator-crud.md`, `docs/superpowers/plans/2026-09-02-projthread-empty-tenant.md`, `docs/superpowers/plans/2026-09-02-projthread-config-mcp.md`, `docs/superpowers/plans/2026-09-03-projthread-payload-json.md`, `docs/superpowers/plans/2026-09-03-projthread-node-project.md`, `docs/superpowers/plans/2026-09-04-projthread-notify.md`, `docs/superpowers/plans/2026-09-04-projthread-blob-http.md`, `docs/superpowers/plans/2026-09-04-projthread-blob-pwa.md`, `docs/superpowers/plans/2026-09-04-projthread-capture-extension.md`.
**Index:** `docs/superpowers/plans/2026-08-26-projthread-v1.md`
**Spec:** `docs/superpowers/specs/2026-08-26-projthread-v1-design.md` (v1). Capture/ingest: `docs/superpowers/specs/2026-09-03-projthread-capture-design.md`.
