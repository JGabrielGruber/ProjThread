# Status

Update this file when a slice lands. Do not put slice history in root `AGENTS.md`.

**Live:** local wrangler — Farm seed, membership, kanban moves (`stage_changed`), room chat + Activity markers on DO tape (reconnect last_seq), Activity-only from D1, wiki markdown nodes + work-item links (no promote), node–node `includes` + `ref` HTTP (`GET` outline/cites without child content; attach still `node_work_item`), PWA Config (members, create/rename projects, stage labels/order; our Vue, no PrimeVue), tokenized Grok/X-sharp skin (dark-first + light, `pt-theme`), chrome shell (left rail on desktop, compact top nav on small screens), overlay status toast, wiki create in Modal and edit filling the stage, kanban cards without on-card stage select (Move + reason in Modal), installable PWA (manifest + SW; SW does not intercept `/api/*` or WebSocket), app HTTP `Authorization: Bearer <session.id>` (same D1 session as `pt_session`; no cookie fallback when Bearer is present), admin **Enter as** + **Issue token** (`set_cookie: false`), same-origin `/mcp` façade (stateless Streamable HTTP; Bearer only; briefing, wiki/card search, Activity log/recent, implicit workspace when one membership; node markdown still `content[0]`), wiki **pins** on `session_briefing` (`{ id, title, type, summary }`, no bodies; human PWA toggle; MCP instructions tell the bot to read pins). Remote Worker: https://projthread.jgabrielgruber.workers.dev (D1 + Room DO; Farm seed; Access on `/admin*` and `/api/admin/*`; chrome shell + toast + wiki edit surface; node-rel `includes`/`ref` HTTP; app HTTP Bearer + admin Issue token; `/mcp` façade with briefing, wiki/card search, Activity, implicit workspace; version `890a2886-c2db-424f-a70d-bdbd23dcb1b4`; no PrimeVue; **pins not deployed**). Custom-domain Deploy still parked. Remote D1 has `0005_node_rel`.
**Now:** no open slice. Next named work is **PWA structure** (plan 15 not written). Park Deploy. Do not start OAuth. Do not start room MCP. Do not start operator CRUD, empty-tenant, or Config MCP until STATUS names them.
**Next:** write the PWA-structure plan (`pages/`, `components/`, `models/`, `services/`, real router; extract primitives; no new CRUD). Then operator CRUD, then empty tenant, then Config MCP. Custom-domain Deploy still waits on a domain.
**Parked (product):** PWA outline / attachment chrome (after structure, with operator CRUD). Node-rel **HTTP shipped**. Session Bearer **HTTP + admin Issue token shipped**. Catalog MCP **façade shipped** (local + workers.dev). Briefing **pins shipped** (local; remote waits on deploy + `0006`). Distinct agent OAuth tokens. Room MCP. Operator CRUD (members remove/role, reparent, owner picker, workspace create in PWA, …). Empty-tenant / drop Farm D1. Config MCP. Ontology, graph canvas, child rooms stay absences. HTTP-wrap MCP names (replaced). PrimeVue stays out.
**Operator (not a plan):** Reset Grok Bot memory (wrap-era bias). Do not drop remote D1 until workspace-setup exists. Apply `0006_wiki_pin` on remote D1 when deploying pins.
**Landed plans:** `docs/superpowers/plans/2026-08-26-projthread-catalog.md`, `docs/superpowers/plans/2026-08-26-projthread-room.md`, `docs/superpowers/plans/2026-08-26-projthread-activity.md`, `docs/superpowers/plans/2026-08-26-projthread-wiki.md`, `docs/superpowers/plans/2026-08-26-projthread-config.md`, `docs/superpowers/plans/2026-08-27-projthread-skin.md`, `docs/superpowers/plans/2026-08-27-projthread-chrome.md`, `docs/superpowers/plans/2026-08-28-projthread-node-rel.md`, `docs/superpowers/plans/2026-08-28-projthread-session-bearer.md`, `docs/superpowers/plans/2026-08-28-projthread-catalog-mcp.md`, `docs/superpowers/plans/2026-08-28-projthread-catalog-mcp-harden.md`, `docs/superpowers/plans/2026-08-31-projthread-mcp-facade.md`, `docs/superpowers/plans/2026-08-31-projthread-briefing-pins.md`
**Index:** `docs/superpowers/plans/2026-08-26-projthread-v1.md`
**Spec:** `docs/superpowers/specs/2026-08-26-projthread-v1-design.md`
