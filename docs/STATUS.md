# Status

Update this file when a slice lands. Do not put slice history in root `AGENTS.md`.

**Live:** local wrangler — empty-tenant setup (session, zero memberships → create workspace); Farm seed retired from the repo; kanban moves (`stage_changed`), room chat + Activity markers on DO tape (reconnect last_seq), Activity-only from D1, wiki markdown nodes + work-item links (no promote), node–node `includes` + `ref` HTTP (`GET` outline/cites without child content; attach still `node_work_item`), reverse attach `GET`/`POST /api/work-items/:id/nodes`, PWA Config (members add/role/remove, create/rename/reparent projects, stage labels/order, workspace create; our Vue, no PrimeVue), session `workspace_id` (picker `PATCH /api/me`; not a URL place), project tree filter chrome on kanban/wiki/room (desktop right; mobile Filters ≤80dvh), room owner picker + nodes of the card, tokenized Grok/X-sharp skin (dark-first + light, `pt-theme`), chrome shell (left rail on desktop, compact top nav on small screens), overlay status toast, wiki create in Modal and edit filling the stage, kanban cards without on-card stage select (Move + reason in Modal), installable PWA (manifest + SW; SW does not intercept `/api/*` or WebSocket), app HTTP `Authorization: Bearer <session.id>` (same D1 session as `pt_session`; no cookie fallback when Bearer is present), admin **Enter as** + **Issue token** (`set_cookie: false`), same-origin `/mcp` façade (stateless Streamable HTTP; Bearer only; briefing, wiki/card search, Activity log/recent, bound session workspace or implicit when one membership; node markdown still `content[0]`; Config tools: workspace create, members list/add/role/remove, project create/rename/reparent, stages replace; wrap catalog HTTP; `principal_id` not display name), wiki **pins** on `session_briefing` (`{ id, title, type, summary }`, no bodies; human PWA toggle; MCP instructions tell the bot to read pins), PWA routes `/` `/wiki` `/config` `/room/:itemId` (`src/app` pages/components/models/services; `apiJson`; Worker `handleAppShell`; no query-flag rewrite). Remote Worker: https://projthread.jgabrielgruber.workers.dev (D1 + Room DO; Farm **rows** remain; Access on `/admin*` and `/api/admin/*`; chrome shell + toast + wiki edit surface; node-rel `includes`/`ref` HTTP; app HTTP Bearer + admin Issue token; `/mcp` façade with briefing, wiki/card search, Activity, bound session workspace; PWA routes + operator CRUD + empty-tenant setup; version `2be119b6-9da2-43f5-b07a-692acfc5fb9d`; no PrimeVue). Custom-domain Deploy still parked. Remote D1 has `0007_session_workspace` (includes `0006_wiki_pin`).
**Now:** no open slice. Park Deploy. Do not start OAuth. Do not start room MCP. Do not mint principals. Do not add a PWA people picker. Do not drop remote D1 unless José asks.
**Plan:** none
**Next:** wait (Deploy parked; OAuth and room MCP absences).
**Parked (product):** Node-rel **HTTP shipped**. Session Bearer **HTTP + admin Issue token shipped**. Catalog MCP **façade shipped** (local + workers.dev). Briefing **pins shipped** (local + workers.dev). Operator CRUD **shipped** (local + workers.dev). Empty-tenant **shipped** (local + workers.dev). Config MCP **shipped** (local; workers.dev until next deploy). Farm seed file gone. Remote Farm rows remain until José asks. Distinct agent OAuth tokens. Room MCP. Ontology, graph canvas, child rooms stay absences. HTTP-wrap MCP names (replaced). PrimeVue stays out.
**Operator (not a plan):** Reset Grok Bot memory (wrap-era bias). Farm seed file gone; leftover local/remote Farm **rows** wait for José. Do not drop remote D1 unless José asks. Remote `0006_wiki_pin` and `0007_session_workspace` applied.
**Landed plans:** `docs/superpowers/plans/2026-08-26-projthread-catalog.md`, `docs/superpowers/plans/2026-08-26-projthread-room.md`, `docs/superpowers/plans/2026-08-26-projthread-activity.md`, `docs/superpowers/plans/2026-08-26-projthread-wiki.md`, `docs/superpowers/plans/2026-08-26-projthread-config.md`, `docs/superpowers/plans/2026-08-27-projthread-skin.md`, `docs/superpowers/plans/2026-08-27-projthread-chrome.md`, `docs/superpowers/plans/2026-08-28-projthread-node-rel.md`, `docs/superpowers/plans/2026-08-28-projthread-session-bearer.md`, `docs/superpowers/plans/2026-08-28-projthread-catalog-mcp.md`, `docs/superpowers/plans/2026-08-28-projthread-catalog-mcp-harden.md`, `docs/superpowers/plans/2026-08-31-projthread-mcp-facade.md`, `docs/superpowers/plans/2026-08-31-projthread-briefing-pins.md`, `docs/superpowers/plans/2026-08-31-projthread-pwa-structure.md`, `docs/superpowers/plans/2026-09-01-projthread-operator-crud.md`, `docs/superpowers/plans/2026-09-02-projthread-empty-tenant.md`, `docs/superpowers/plans/2026-09-02-projthread-config-mcp.md`
**Index:** `docs/superpowers/plans/2026-08-26-projthread-v1.md`
**Spec:** `docs/superpowers/specs/2026-08-26-projthread-v1-design.md`
